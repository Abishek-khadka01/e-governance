import { prisma } from '../../app';

export class DocumentService {
  /*
        Document Verification by  adminm 
        Add the document of the user (offline registration ),
         Get all the documents per user 
    */
}
